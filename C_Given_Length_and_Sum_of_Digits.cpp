
#include<bits/stdc++.h>
using namespace std;

using ll = long long;

int main ()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int m,s;
    cin>>m>>s;

    if(s < m)
    {
        cout<<-1<<" "<<-1<<'\n';
        return 0;
    }
    else
    {
        
    }
    
}
