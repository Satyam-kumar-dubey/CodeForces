
#include<bits/stdc++.h>
using namespace std;

using ll = long long;

int main ()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int n,m;
    cin>>n>>m;

    int t;
    while(t--)
    {
        int tp;
        cin>>tp;
        vector<int>v(tp);
        for(int i=0; i<tp; i++)
        cin>>v[i];
    }
    
}
